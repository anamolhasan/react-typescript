import React from 'react'

const Counter = () => {

  return (
    <div>
        <p>Count : {}</p>
        <p>Render Count : {}</p>
        <button onClick={}>Increase</button>
    </div>
  )
}

export default Counter